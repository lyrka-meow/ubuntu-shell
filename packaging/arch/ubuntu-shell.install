post_install() {
  cat <<'EOF'

Ubuntu Shell was installed.

Run:
  ubuntu-shell

The first launch configures Docker automatically and may ask for your sudo
password. Later logins will run without sudo.
EOF
}
